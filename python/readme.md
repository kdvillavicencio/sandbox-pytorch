# Python

## Setup virtual environment
Create venv directory
```
$ python -m venv env
```
Activate venv
```
$ cd env
$ source bin/activate
```
Install dependencies
```
$ pip install -r <requirements.txt>
```